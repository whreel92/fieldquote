import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function CaptureScreen() {
  return <PlaceholderScreen title="Capture" phase="Phase 4 (photos, dictation, offline queue)" />;
}
